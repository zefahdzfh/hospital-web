<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Edit Reservasi</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Edit Reservasi</h1>
    <form action="/reservasi/<%= reservasi.id_reservasi %>/edit" method="POST">
      <div class="mb-3">
        <label for="no_rm" class="form-label">No RM</label>
        <input type="number" class="form-control" id="no_rm" name="no_rm" value="<%= reservasi.no_rm %>" required>
      </div>
      <div class="mb-3">
        <label for="id_poli" class="form-label">ID Poli</label>
        <input type="number" class="form-control" id="id_poli" name="id_poli" value="<%= reservasi.id_poli %>" required>
      </div>
      <div class="mb-3">
        <label for="id_dokter" class="form-label">ID Dokter</label>
        <input type="number" class="form-control" id="id_dokter" name="id_dokter" value="<%= reservasi.id_dokter %>" required>
      </div>
      <div class="mb-3">
        <label for="no_antrian" class="form-label">No Antrian</label>
        <input type="number" class="form-control" id="no_antrian" name="no_antrian" value="<%= reservasi.no_antrian %>" required>
      </div>
      <div class="d-flex">
        <button type="submit" class="btn btn-primary me-2">Update</button>
        <a href="/reservasi" class="btn btn-secondary">Kembali ke Daftar Reservasi</a>
      </div>
    </form>
  </div>

  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
